export function http(options) {
    const check = {
        type: "http",
        name: options.name,
        request: options.request,
    };
    if (options.expect !== undefined) {
        check.expect = options.expect;
    }
    return check;
}
//# sourceMappingURL=http.js.map